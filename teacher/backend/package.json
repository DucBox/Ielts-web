{
  "name": "ielts-teacher-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "stt-proxy:dev": "node src/stt-proxy.mjs"
  },
  "dependencies": {
    "@fastify/busboy": "^3.1.1",
    "@neondatabase/serverless": "^0.10.4",
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
